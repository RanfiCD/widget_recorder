name: widget_recorder
description: Generates images from a Widget.
version: 0.1.0+1
homepage: https://github.com/RanfiCD/widget_recorder

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true